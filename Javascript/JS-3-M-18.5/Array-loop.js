//  // Array loop
var numbers = [23, 33, 45, 64, 32, 87, 98, 82]
for (i = 0; i < numbers.length; i++) {
    var element = numbers[i]
    console.log(element);
}

// 
var items = ['bottle', 'mobile', 'charger', 'watch', 'book', 'paper']
for (i = 0; i < items.length; i++) {
    var item = items[i]
    console.log(item)
}